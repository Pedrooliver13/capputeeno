'use client';

// Packages
import { ReactElement } from 'react';

// Templates
import { ShowTemplate } from 'templates';

const Show = (): ReactElement => {
  return <ShowTemplate />;
};

export default Show;
