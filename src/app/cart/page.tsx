'use client';

// Packages
import { ReactElement } from 'react';

// Templates
import { CartTemplate } from 'templates';

const Cart = (): ReactElement => {
  return <CartTemplate />;
};

export default Cart;
